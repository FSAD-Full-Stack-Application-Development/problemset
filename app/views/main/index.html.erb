<!DOCTYPE html>
<html style="height: auto;">
<head>
    <style>
        html, body {
            height: auto;
            overflow-x: hidden;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 20px;
            background: linear-gradient(135deg, #a8e6cf 0%, #ffd3a5 100%);
            min-height: 100vh;
            overflow-y: auto;
        }
        .container {
            max-width: 1000px;
            margin: 0 auto 40px auto;
            background: white;
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.1);
            overflow: visible;
            min-height: auto;
        }
        .header {
            background: linear-gradient(135deg, #88c999 0%, #ffb347 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }
        .main-title {
            font-size: 2.2em;
            font-weight: bold;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }
        .course-info {
            font-size: 1.1em;
            opacity: 0.9;
            letter-spacing: 1px;
        }
        .team-members {
            display: flex;
            justify-content: space-around;
            padding: 20px;
            flex-wrap: wrap;
        }
        .member-card {
            background: white;
            border-radius: 6px;
            padding: 15px 20px;
            margin: 8px;
            box-shadow: 0 2px 8px rgba(136, 201, 153, 0.15);
            text-align: center;
            flex: 1;
            min-width: 180px;
            max-width: 240px;
            transition: all 0.2s ease;
            border: 1px solid #e8f5e8;
        }
        .member-card:hover {
            transform: translateY(-2px);
            border-color: #88c999;
            box-shadow: 0 4px 12px rgba(136, 201, 153, 0.2);
        }
        .member-name {
            font-size: 1.1em;
            font-weight: 600;
            color: #2d5016;
            margin-bottom: 8px;
            line-height: 1.3;
        }
        .member-id {
            font-size: 0.9em;
            color: #7a7a7a;
            font-weight: 400;
            font-family: 'Courier New', monospace;
        }
        .problem-sets {
            padding: 40px 30px 0px 30px;
            background: white;
        }
        .team-info {
            padding: 10px 30px;
            background: linear-gradient(135deg, #f8fcf8 0%, #fffcf9 100%);
        }
        .problem-card {
            border: 2px solid #e8f5e8;
            border-radius: 12px;
            padding: 20px;
            margin: 15px auto;
            max-width: 600px;
            transition: all 0.3s ease;
            background: linear-gradient(135deg, #f8fff8 0%, #fffaf7 100%);
            box-shadow: 0 3px 10px rgba(136, 201, 153, 0.1);
        }
        .problem-card:hover {
            border-color: #88c999;
            box-shadow: 0 8px 25px rgba(136, 201, 153, 0.2);
            transform: translateY(-3px);
        }
        .problem-title {
            font-size: 1.8em;
            font-weight: bold;
            margin-bottom: 15px;
        }
        .problem-title a {
            text-decoration: none;
            color: #2d5016;
            transition: all 0.3s ease;
            display: block;
            padding: 10px;
            border-radius: 8px;
        }
        .problem-title a:hover {
            color: #cc7a00;
            background: rgba(136, 201, 153, 0.1);
        }
        .problem-description {
            color: #5a6b47;
            margin-bottom: 20px;
            line-height: 1.6;
            font-size: 1.1em;
        }
        .access-link {
            display: inline-block;
            padding: 12px 30px;
            background: linear-gradient(135deg, #88c999, #ffb347);
            color: white;
            text-decoration: none;
            border-radius: 30px;
            transition: all 0.3s ease;
            font-weight: 600;
            font-size: 1.1em;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.2);
        }
        .access-link:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(136, 201, 153, 0.4);
            background: linear-gradient(135deg, #76b887, #e6a034);
        }
        .footer {
            background: linear-gradient(135deg, #e8f5e8 0%, #fff2e6 100%);
            padding: 20px;
            text-align: center;
            color: #5a6b47;
            font-size: 0.9em;
            margin-bottom: 40px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="main-title">Team Yolanda_Sake</div>
            <div class="course-info">FSAD - Full Stack Application Development</div>
            <div style="margin-top: 20px;">
                <a href="/project" class="access-link" style="display: inline-block;">📁 Project</a>
            </div>
        </div>
        
        <div class="problem-sets">
            <h1 style="text-align: center; color: #2d5016; margin-bottom: 30px; font-size: 2em;">Problem Sets & Solutions</h1>
            <% @problems.each do |problem| %>
                <div class="problem-card">
                    <div class="problem-title">
                        <a href="<%= problem[:path] %>"><%= problem[:title] %></a>
                    </div>
                    <div class="problem-description">
                        <%= problem[:description] %>
                    </div>
                    <div style="text-align: center; margin-top: 20px;">
                        <a href="<%= problem[:path] %>" class="access-link">🔗 Access Solution</a>
                    </div>
                </div>
            <% end %>
        </div>
        
        <div class="team-info">
            <h3 style="text-align: center; color: #2d5016; margin-bottom: 20px; font-size: 1.3em; font-weight: 600;">Team Members</h3>
            <div class="team-members">
                <div class="member-card">
                    <div class="member-name">Aye Khin Khin Hpone<br><span style="font-size: 0.9em; color: #5a6b47;">(Yolanda Lim)</span></div>
                    <div class="member-id">Student ID: 125970</div>
                </div>
                <div class="member-card">
                    <div class="member-name">Saugat Shakya</div>
                    <div class="member-id">Student ID: 125986</div>
                </div>
            </div>
        </div>
        
        <div class="footer">
            Generated at <%= Time.current.strftime("%Y-%m-%d %H:%M:%S") %> | 
            Rails Environment: <%= Rails.env.upcase %> | 
            Ruby Version: <%= RUBY_VERSION %>
        </div>
    </div>
</body>
</html>
